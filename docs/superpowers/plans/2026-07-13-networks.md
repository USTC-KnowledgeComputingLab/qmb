# Networks 子系统实现计划

**Goal:** 实现 `src/qmp/networks/` — MLP + Transformers 自回归 NQS，各含 Normal/Electron/ElectronUpDown 三变体，配套 JAX 版 `utility/bitspack.py`。

**Spec:** `docs/superpowers/specs/2026-07-13-networks-design.md`

**Tech Stack:** jax, flax (nnx), pytest, ruff, ty

**约定:** `from __future__ import annotations`；`X | None`；`list[X]`；描述性变量名（禁单字母大写）；双引号；行长 120；零 suppress；TDD。

---

### Task 1: 依赖 + x64 启用

**Files:**
- Edit: `pyproject.toml` — 依赖组加 `flax`
- Edit: `src/qmp/__init__.py` — 启用 `jax_enable_x64`

- [ ] Step 1: `pyproject.toml` `dependencies` 增加 `"flax>=0.12.0"`
- [ ] Step 2: `src/qmp/__init__.py` 在导入 `_version` 前 `import jax; jax.config.update("jax_enable_x64", True)`
- [ ] Step 3: `uv sync`
- [ ] Step 4: 验证 `uv run python -c "from flax import nnx; import jax.numpy as jnp; jax.config...; print(jnp.zeros(1,dtype=jnp.float64).dtype)"` 输出 float64
- [ ] Step 5: 跑现有测试确保 x64 不破坏 Hamiltonian 测试
- [ ] Step 6: Commit

---

### Task 2: utility/bitspack.py + 测试 (TDD)

**Files:**
- Write: `tests/unit/utility/__init__.py`
- Write: `tests/unit/utility/test_bitspack.py`
- Write: `src/qmp/utility/__init__.py`
- Write: `src/qmp/utility/bitspack.py`

- [ ] Step 1: 写测试：pack∘unpack 往返（size ∈ {1,2,4,8}）；padding 边界；已知小例位布局手工验证
- [ ] Step 2: 实现 `pack_int` / `unpack_int`（纯 jax.numpy，LSB-first）
- [ ] Step 3: `uv run pytest tests/unit/utility -v` 全绿
- [ ] Step 4: ruff/ty
- [ ] Step 5: Commit

---

### Task 3: networks/_protocol.py + _autoregressive.py + 测试 (TDD)

**Files:**
- Write: `tests/unit/networks/__init__.py`
- Write: `tests/unit/networks/test_autoregressive.py`
- Write: `src/qmp/networks/__init__.py` (先占位)
- Write: `src/qmp/networks/_protocol.py`
- Write: `src/qmp/networks/_autoregressive.py`

- [ ] Step 1: 写测试：
  - `normalize_log_amplitude` 后 Σ exp(2x)=1
  - `mask_electron` / `mask_electron_up_down` 手工小例
  - `gumbel_topk_step` 无 NaN（含 sentinel 无效分支）
  - gumbel 束搜索唯一性 + K 上限
- [ ] Step 2: 实现 `_protocol.py` (NetworkProto Protocol)
- [ ] Step 3: 实现 `_autoregressive.py`:
  - `normalize_log_amplitude(log_amp, axes)`
  - `mask_electron(...)`, `mask_electron_up_down(...)`
  - `gumbel_topk_step(...)`（sentinel + where 护栏）
  - `sample_step(...)`（categorical）
- [ ] Step 4: `uv run pytest tests/unit/networks/test_autoregressive.py -v`
- [ ] Step 5: ruff/ty
- [ ] Step 6: Commit

---

### Task 4: networks/mlp.py 三变体 + 测试 (TDD)

**Files:**
- Write: `tests/unit/networks/test_mlp.py`
- Write: `src/qmp/networks/mlp.py`
- Edit: `src/qmp/networks/__init__.py` (re-export MLP 变体)

- [ ] Step 1: 写测试（三变体）：
  - `__call__` shape=[batch]、dtype=complex128
  - 归一化自洽（枚举全空间 Σ|ψ|²=1）
  - 粒子数守恒（违反 → ψ=0）
  - generate_unique psi 与 __call__ 自洽
  - generate_unique 唯一性 + ≤ batch_size
  - generate counts 和 = batch_size
  - PRNG 确定性（同 key 复现）
  - ordering +1/-1/自定义
- [ ] Step 2: 实现 `MLP` 基础模块（nnx，dim_in==0 bias-only）
- [ ] Step 3: 实现 `WaveFunctionNormal`（任意 physical_dim）
- [ ] Step 4: 实现 `WaveFunctionElectron`
- [ ] Step 5: 实现 `WaveFunctionElectronUpDown`
- [ ] Step 6: `uv run pytest tests/unit/networks/test_mlp.py -v`
- [ ] Step 7: ruff/ty
- [ ] Step 8: Commit

---

### Task 5: networks/transformers.py 三变体 + 测试 (TDD)

**Files:**
- Write: `tests/unit/networks/test_transformers.py`
- Write: `src/qmp/networks/transformers.py`
- Edit: `src/qmp/networks/__init__.py` (re-export Transformers 变体)

- [ ] Step 1: 写测试（同 mlp 覆盖矩阵，三变体）
- [ ] Step 2: 实现 building blocks: `Embedding`, `SelfAttention`(KV-cache, stop_gradient), `FeedForward`(GELU), `DecoderUnit`(pre-norm), `Transformers`(depth), `Tail`
- [ ] Step 3: 实现 `WaveFunctionNormal`（任意 physical_dim）
- [ ] Step 4: 实现 `WaveFunctionElectron`
- [ ] Step 5: 实现 `WaveFunctionElectronUpDown`
- [ ] Step 6: `uv run pytest tests/unit/networks/test_transformers.py -v`
- [ ] Step 7: ruff/ty
- [ ] Step 8: Commit

---

### Task 6: 收尾 SOP

- [ ] Step 1: `uv run ruff format .`
- [ ] Step 2: `uv run ruff check .`
- [ ] Step 3: `uv run ty check src tests`
- [ ] Step 4: `uv run pytest -v`（全部，含 Hamiltonian 回归）
- [ ] Step 5: 更新 AGENTS.md（如需，记录 networks 子系统 + x64 策略）
- [ ] Step 6: Commit

---

## 命名约定检查清单（每个文件）

- [ ] `from __future__ import annotations`
- [ ] 描述性全小写变量名（`batch_size`, `n_qubytes`, `site_index`, `count_selected`, `beam_width`）
- [ ] 无单字母大写变量（`B`/`T`/`K`/`L` 等）→ `batch_size`/`beam_width`/`log_prob`/`perturbed`
- [ ] 双引号
- [ ] 无 `# noqa` / `# ty: ignore`（除非第三方无 stub，附理由）
